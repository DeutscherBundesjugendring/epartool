'use strict';

module.exports = {

  css: '<%= paths.dist %>/css/*',
  js: '<%= paths.dist %>/js/*',
  fonts: '<%= paths.dist %>/font/*',
  //images: '<%= paths.dist %>/images/*',
  temp: '<%= paths.temp %>'

};
