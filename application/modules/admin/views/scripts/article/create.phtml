<section class="section-header has-tabs">
    <div class="container">

        <?php if (isset($this->consultation) && !empty($this->consultation)): ?>
            <h1><?=$this->consultation['titl'];?></h1>
            <h2><?=$this->consultation['titl_sub'];?></h2>
            <?=$this->consultationTabs($this->consultation['kid'], 'consultation');?>
        <?php else: ?>
            <h1>General Settings</h1>
        <?php endif; ?>

    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="col-md-3">

                <?php if (isset($this->consultation) && !empty($this->consultation)): ?>
                    <?=$this->consultationNavigation('info');?>
                <?php else: ?>
                    <?=$this->settingsNavigation('pages');?>
                <?php endif; ?>

            </div>
            <div class="col-md-9">

                <?php if (isset($this->consultation) && !empty($this->consultation)): ?>
                    <h2>New Article</h2>
                <?php else: ?>
                    <h2>New Page</h2>
                <?php endif; ?>

                <?php echo $this->form;?>
                <p>
                    or
                    <a href="<?=$this->url(array('action' => 'index', (isset($this->consultation) && !empty($this->consultation)) ? $this->kid : null));?>">Cancel</a>
                </p>

            </div>
        </div>
    </div>
</section>

<!-- TODO move to JS -->
<script>
    CKEDITOR.replace( 'artcl' );
    CKEDITOR.replace( 'sidebar' );
</script>
