<section class="section-header has-tabs">
    <div class="container">
        <div class="row">
            <div class="col-sm-8">
                <h1><?=$this->translate('Email');?></h1>
            </div>
            <div class="col-sm-4">
                <a href="<?=$this->url(array('controller' => 'mail-send', 'action' => 'index'));?>" class="btn btn-primary cta"><span class="glyphicon glyphicon-pencil"></span> <?=$this->translate('New Email');?></a>
            </div>
        </div>
        <?=$this->emailTabs('emailing');?>
    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="col-md-3">

                <?=$this->emailNavigation('sent');?>

            </div>
            <div class="col-md-9">

                <h2><?=$this->translate('Sent Emails');?></h2>

                <form method="<?=$this->form->getMethod()?>">
                    <?=$this->form->getElement('list_control_csrf_token')?>
                    <table class="table" data-view="table">
                        <thead>
                            <tr>
                                <th class="col-xs-3" data-toggle="sort" data-order="default desc" tabindex="0"><?=$this->translate('Time Sent');?></th>
                                <th data-toggle="sort" tabindex="0"><?=$this->translate('Subject');?></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($this->emails as $email): ?>
                                <tr>
                                    <td>
                                        <?php
                                        $zendDate = new Zend_Date($email->time_sent);
                                        echo $zendDate->get(Zend_Date::DATETIME_MEDIUM);
                                        ?>
                                    </td>
                                    <td><?=$email->subject;?></td>
                                    <td>
                                        <button
                                            name="resendId"
                                            type="submit"
                                            onclick="return confirm('<?=$this->translate('Are you sure?');?>');"
                                            value="<?=$email->id;?>"
                                            title="<?=$this->translate('Resend email');?>"
                                            >
                                            <span class="glyphicon glyphicon-share-alt"></span>
                                        </button>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </form>

            </div>
        </div>
    </div>
</section>
