<section class="section-header has-tabs">
    <div class="container">

        <h1><?=$this->consultation['titl'];?></h1>
        <h2><?=$this->consultation['titl_sub'];?></h2>

        <?=$this->consultationTabs($this->consultation['kid'], 'consultation');?>

    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="col-md-3">

                <?=$this->consultationNavigation('followup');?>

            </div>
            <div class="col-md-9">

                <a href="<?=$this->url(array('action' => 'create-file', 'kid' => $this->consultation['kid']));?>" class="btn btn-default pull-right">
                    <span class="glyphicon glyphicon-plus-sign"></span>
                    New Follow Up
                </a>
                <h2>Follow Up</h2>

                <table class="table">
                    <tbody>
                    <tr>
                        <th>Datum</th>
                        <th>Akteur_in</th>
                        <th>Titel</th>
                        <th>Aktionen</th>
                    </tr>
                    <?php foreach ($this->followupFiles as $followupFile): ?>
                        <tr>

                            <td><?php $zendDate = new Zend_Date($followupFile['when']); echo $zendDate->get('dd.MM.yy'); ?></td>
                            <td><?php echo $followupFile['who']; ?></td>
                            <td><?php echo $followupFile['titl']; ?></td>
                            <td><a href="<?php echo $this->url(array(
                                    'action' => 'edit-file',
                                    'kid' => $this->consultation['kid'],
                                    'ffid' => $followupFile['ffid']
                                ));?>">Bearbeiten</a>
                                &nbsp;
                                <a href="<?php echo $this->url(array(
                                    'action' => 'delete-file',
                                    'kid' => $this->consultation['kid'],
                                    'ffid' => $followupFile['ffid']
                                ));?>" title="Löschen">Löschen</a>
                            </td>

                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</section>
