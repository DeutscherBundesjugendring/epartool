<section class="section-header has-tabs">
    <div class="container">

        <h1><?=$this->consultation['titl'];?></h1>
        <h2><?=$this->consultation['titl_sub'];?></h2>

        <?=$this->consultationTabs($this->consultation['kid'], 'consultation');?>

    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="col-md-3">

                <?=$this->consultationNavigation('voting-permissions');?>

            </div>
            <div class="col-md-9">

                <h2>Voting Permissions</h2>

                <p>Seit dem letzten Aufruf wurden <?php echo $this->countInserted; ?> neue Nutzer_innen zu dieser Liste hinzugefügt:</p>
                <table class="table">
                    <tr>
                        <th>uid</th>
                        <th>Benutzer/-in</th>
                        <th>Gewicht</th>
                        <th>Zugangscode</th>
                        <th>Anerkannte Größe</th>
                        <th>Größe lt. eig. Angabe</th>
                        <th>Aktionen</th>
                    </tr>
                    <?php foreach ($this->votingRights as $item): ?>
                        <tr>
                            <td><?php echo $item['uid']; ?></td>
                            <td><a href="<?php echo $this->url(array(
                                    'controller' => 'input',
                                    'action' => 'list',
                                    'uid' => $item['uid'],
                                )); ?>" title="Alle Beiträge dieses Nutzers anzeigen"><?php echo $item['email']; ?></a></td>
                            <td><?php echo $item['vt_weight']; ?></td>
                            <td><?php echo $item['vt_code']; ?></td>
                            <td><?php echo $item['grp_siz']; ?></td>
                            <td><?php echo $item['group_size_user']; ?></td>
                            <td><a href="<?php echo $this->url(array(
                                    'action' => 'editrights',
                                    'uid' => $item['uid']
                                )); ?>">Bearbeiten</a></td>
                        </tr>
                    <?php endforeach; ?>
                </table>

            </div>
        </div>
    </div>
</section>
