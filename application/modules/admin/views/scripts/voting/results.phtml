<section class="section-header has-tabs">
    <div class="container">

        <h1><?=$this->consultation['titl'];?></h1>
        <h2><?=$this->consultation['titl_sub'];?></h2>

        <?=$this->consultationTabs($this->consultation['kid'], 'consultation');?>

    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="sidebar">

                <?=$this->consultationNavigation($this->consultation['kid'], 'voting-results');?>

            </div>
            <div class="content">

                <h2><?=$this->translate('Voting Results');?></h2>

                <h3>Fragen:</h3>
                <ul>
                    <?php foreach ($this->questions as $question): ?>
                        <?php if ($question['qi'] == $this->currentQuestion['qi']): ?>
                            <li><strong><?=$question['nr'] . ' ' . $question['q'];?></strong></li>
                        <?php else: ?>
                            <li><a href="<?=$this->url(array(
                                    'qid' => $question['qi']
                                ));?>"><?=$question['nr'] . ' ' . $question['q'];?></a></li>
                        <?php endif;?>
                    <?php endforeach;?>
                </ul>

                <h3><?=$this->currentQuestion['nr'] . ' ' . $this->currentQuestion['q'];?></h3>
                <?php foreach ($this->theses_votes as $rank => $theses): ?>
                    <?php $diag_lngth = round(($rank*600)/$this->highest_rank, 0);?>
                    <?php foreach ($theses as $thesis): ?>
                        <p><?=$thesis['thes'];?></p>
                        <div
                            style="background-color: #d9e8ef; color: #3f6f8d; font-size: smaller; margin-bottom: 2em; padding: 4px; width: <?=$diag_lngth;?>px;"
                            class="voting-result-bar<?=$thesis['followup'];?>"
                            title="(Gewichtete Punkte: <?=round($thesis['points'], 2);?> bei <?=$thesis['cast'];?> Stimmen)"
                        >
                            &nbsp;<?=round($rank, 2);?> Punkte
                        </div>
                    <?php endforeach;?>
                <?php endforeach;?>

            </div>
        </div>
    </div>
</section>
