<section class="section-header">
    <div class="container">
        <div class="row">
            <div class="col-sm-8">
                <h1>Users</h1>
            </div>
            <div class="col-sm-4">
                <a href="<?=$this->url(array('action' => 'create'));?>" class="btn btn-primary cta"><span class="glyphicon glyphicon-plus-sign"></span> New User</a>
            </div>
        </div>
    </div>
</section>
<section class="section-content">
    <div class="container">

        <table class="table">
            <tr>
                <th>Nr.</th>
                <th>Name</th>
                <th>E-mail-Adresse</th>
                <th>Rolle</th>
                <th>Letzte Aktivit&auml;t</th>
                <th>Aktionen</th>
            </tr>
            <?php
            $i=1;
            foreach ($this->userlist as $user):?>
                <tr>
                    <td><?php echo $i; ?></td>
                    <td><?php echo (empty($user->name))? '<i>n/a</i>' : $user->name; ?></td>
                    <td><?php echo (empty($user->email))? '<i>n/a</i>' : $user->email; ?></td>
                    <td><?php
                        switch($user->lvl) {
                            case 'usr':
                                $ulvl = 'Benutzer';
                                break;
                            case 'edt':
                                $ulvl = 'Redakteur';
                                break;
                            case 'adm':
                                $ulvl = 'Administrator';
                                break;
                        }
                        echo $ulvl;
                        ?></td>
                    <td><?php
                        $zendDate = new Zend_Date($user->last_act);
                        echo($zendDate->get('dd.MM.yy'));
                        ?></td>
                    <td><a class="icon icon-edit" href="<?php echo $this->url(array(
                            'action' => 'edit',
                            'uid' => $user->uid
                        ));?>">Bearbeiten</a>
                        &nbsp;
                        <a
                            class="icon icon-delete"
                            href="<?php echo $this->url(array('action' => 'delete', 'uid' => $user->uid));?>"
                            title="Benutzer löschen"
                            onclick="return confirm('Are you sure? All inputs will be amde anonymous.')"
                            >
                            Löschen
                        </a>
                </tr>
                <?php
                $i++;
            endforeach;?>
        </table>

    </div>
</section>
