<section class="section-header has-tabs">
    <div class="container">

        <h1><?=$this->consultation->titl;?></h1>
        <h2><?=$this->consultation->titl_sub;?></h2>

        <?=$this->consultationTabs($this->consultation->kid, 'settings');?>

    </div>
</section>
<section class="section-content">
    <div class="container">
        <div class="row">
            <div class="sidebar">

                <?=$this->consultationEditNavigation($this->consultation->kid, 'settings');?>

            </div>
            <div class="content">

                <?php if (Zend_Auth::getInstance()->getIdentity()->lvl === 'adm'): ?>
                    <!-- TODO change link to POST form button -->
                    <a href="<?=$this->url(array('action' => 'delete', $this->consultation->kid));?>" class="pull-right">
                        <span class="glyphicon glyphicon-trash"></span>
                        <?=$this->translate('Delete Consultation');?>
                    </a>
                <?php endif; ?>

                <h2><?=$this->translate('Consultation Settings');?></h2>
                <?=$this->form?>
                <p>
                    <?=$this->translate('or');?>
                    <a href="<?=$this->url(array('action' => 'index', $this->consultation->kid));?>"><?=$this->translate('Cancel');?></a>
                </p>

            </div>
        </div>
    </div>
</section>
