<section class="section-header has-tabs">
    <div class="container">
        <h1><?=$this->translate('New Email');?></h1>
        <?=$this->emailTabs('emailing');?>
    </div>
</section>
<section class="section-content">
    <div class="container">

        <a href="<?=$this->url(['controller' => 'mail-sent', 'action' => 'index']);?>" class="back"><?=$this->translate('Back to Sent Emails');?></a>

        <h3><?=$this->translate('Create from template');?></h3>
        <select class="form-control js-template-selector">
            <option value="0"><?=$this->translate('Please select…');?></option>
            <?php foreach ($this->templates as $template): ?>
                <option value="<?=$this->escape($template->id);?>"><?=$this->escape($template->name);?></option>
            <?php endforeach; ?>
        </select>

        <h3><?=$this->translate('Email');?></h3>
        <?=$this->form;?>

        <div class="well">
            <h4><?=$this->translate('Placeholders');?></h4>
            <table class="table table-striped">
                <?php foreach ($this->placeholders as $placeholder): ?>
                    <tr>
                        <th class="col-sm-3"><?php echo Dbjr_Mail::TOKEN_OPEN . $placeholder->name . Dbjr_Mail::TOKEN_CLOSE; ?></th>
                        <td><?=$this->escape($placeholder->description);?></td>
                    </tr>
                <?php endforeach; ?>
            </table>
        </div>

        <div class="well">
            <h4><?=$this->translate('Components');?></h4>
            <table class="table table-striped">
                <?php foreach ($this->components as $component): ?>
                    <tr>
                        <th class="col-sm-3"><?php echo Dbjr_Mail::TOKEN_OPEN . $component->name . Dbjr_Mail::TOKEN_CLOSE; ?></th>
                        <td><?=$this->escape($component->description);?></td>
                    </tr>
                <?php endforeach; ?>
            </table>
        </div>

    </div>
</section>
