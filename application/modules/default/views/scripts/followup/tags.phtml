<?php echo $this->secondNavigation('follow-up') ?>

<div class="content-holder">
    <div class="row">
        <aside class="span3">
            <?php echo $this->followupNavigation('tags') ?>
        </aside>

        <div class="main-content span9">

            <small class="count muted">
                <?php echo $this->inputCount ?>
                <?=$this->translate('confirmed contributions')?>
            </small>
            <h1><?=$this->translate('All contributions')?></h1>
            <p>
                <?=$this->translate('You want to participate in this participation round? Just click on a question during the input period, and the input field will appear where you can insert your contributions. After the input period has finished, you are no longer allowed to draw up contributions, but by clicking on a question you can read all other contributions.')?>
            </p>

            <!-- Questions -->
            <?php if (!empty($this->tags)): ?>
                <div class="well-large-alt text-center">
                    <h2><?=$this->translate('Contributions sorted by keywords:')?></h2>
                    <ul class="tag-cloud">
                        <?php foreach ($this->tags as $tag): ?>
                            <li class="tag-<?php echo $tag['frequency']; ?>">
                                <a href="<?php echo $this->url(array('action' => 'inputs-by-question', 'tag' => $tag['tg_nr'])); ?>">
                                    <?php echo $tag['tg_de']; ?>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                    <a href="<?php echo $this->url(array('action' => 'inputs-by-question')); ?>" class="btn">
                        <?=$this->translate('Back to overview of questions')?>
                    </a>
                </div><!-- .well-large-alt -->
            <?php endif; ?>

        </div><!-- .main-content -->
    </div><!-- .row -->

    <?php echo $this->render('_partials/printLink.phtml') ?>

</div><!-- .content-holder -->

<?php echo $this->render('_partials/topLink.phtml') ?>
