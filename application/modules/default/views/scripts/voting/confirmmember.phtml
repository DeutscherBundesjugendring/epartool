<div class="main-content content-holder">

    <?php if ($this->act == 'confirm'): ?>
        <h1><?=$this->translate('Dieses Gruppenmitglied ist jetzt bestätigt.')?></h1>
        <p>
            <?=sprintf($this->translate('Vielen Dank für die Bestätigung des Gruppenmitglieds mit der E-Mail-Adresse "%s". Die Stimmen werden nun für die Gruppe gezählt.'), $this->subuser)?>
        </p>
    <?php elseif ($this->act == 'reject'): ?>
        <h1><?=$this->translate('Der/die Abstimmende wurde abgelehnt.')?></h1>
        <p>
            <?=sprintf($this->translate('Die mit der E-Mail-Adresse "%s" abgegebenen Stimmen fließen nicht in das Endergebnis eurer Gruppe ein. Über eure Entscheidung wird die Person nicht informiert, aber ihr könnt diese Person gerne kontaktieren.'), $this->subuser)?>
        </p>
    <?php endif; ?>

    <p>
        <a href="<?php echo $this->url(array('controller' => 'index'), 'default', true); ?>">
            <?=$this->translate('Zurück zur Startseite')?>
        </a>
    </p>

</div><!-- .main-content -->
