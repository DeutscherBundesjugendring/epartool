<?php
$this->layout()->pageTitle = $this->pageTitle(
    Application_View_Helper_PageTitle::STATIC_PAGE,
    $this->translate('New member confirmation')
);
?>
<div class="main-content">

    <?php if ($this->act == 'confirm'): ?>
        <h1><?=$this->translate('This group member has been confirmed.');?></h1>
        <p>
            <?=sprintf($this->translate('Thank you for confirming the group member with the email address "%s". The votes are now being counted for your group.'), $this->subuser);?>
        </p>
    <?php elseif ($this->act == 'reject'): ?>
        <h1><?=$this->translate('Individual vote has been rejected and will not be counted.');?></h1>
        <p>
            <?=sprintf($this->translate('The votings connected to the email address "%s" will not be included in the end results of your group. The person will not receive information about your decision, but you may contact this person if you wish.'), $this->subuser);?>
        </p>
    <?php endif; ?>

    <p>
        <a href="<?=$this->url(['controller' => 'index'], 'default', true);?>" class="btn btn-default">
            <span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span>
            <?=$this->translate('Back to Home');?>
        </a>
    </p>

</div><!-- .main-content -->
